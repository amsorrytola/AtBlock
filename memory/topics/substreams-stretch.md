# Topic: Substreams stretch (The Graph)

> Last updated: 2026-09-11  
> Status: stretch — **not** on the happy-path demo

## Why deferred

AtBlock’s Graph prize story is already load-bearing via Messari Yield Aggregator composition (one query × ≥2 deployments) + AI `SKILL.md`. Substreams one-prompt deploy is optional prize flavor and competes with live pay time.

## If we have spare hours after live qualify

1. Pick a Messari yield / vault Substreams package aligned with our schema.
2. One-prompt deploy per Graph Substreams docs.
3. Mention in README as “optional ingest path” — do **not** replace the Studio gateway cite in the video.

## Non-goal

Do not mock Substreams output for judges. Same fail-closed rule as subgraphs.
