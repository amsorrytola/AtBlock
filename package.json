{
  "name": "atblock",
  "private": true,
  "version": "0.1.0",
  "description": "AtBlock — pay for Graph-cited vault facts on Hedera x402 (ETHOnline 2026)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:gateway": "npm run dev -w @atblock/gateway",
    "dev:web": "npm run dev -w @atblock/web",
    "dev": "npm run dev:gateway & npm run dev:web & wait",
    "build": "npm run build -w @atblock/composer && npm run build -w @atblock/ens && npm run build -w @atblock/gateway && npm run build -w @atblock/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "qualify": "node scripts/qualify.mjs",
    "smoke:facilitator": "npm run smoke:facilitator -w @atblock/gateway"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
